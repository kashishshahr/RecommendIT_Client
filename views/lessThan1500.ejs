<%- include('./partials/head') %>
<%- include('./partials/nav') %>

<section id="lessSec" class="main">
    <% cheapData.forEach(product => { %>
        <figure class="less__fig">
            <a href="/details/<%= product.id %>" target="_blank">
                <img class="less__img" src=<%= product.pictureLink %> alt="">
            </a>
            <figcaption>
                <a class="fig__link" href="/single/<%= product.id %>" target="_blank">
                    <%= product.productName %>
                     <% if(typeof displayName == 'string' && displayName ) {%>
                        <span class="promo">Promo Code: <%= product.promo %></span> 
                        <% }else{ %>  
                            <span class="promo">Login To Get Promo Code</span>
                         <% } %>
                </a>
                <p class="price">₹<%= product.price %></p>
            </figcaption>
        </figure>
    <% }) %>
</section>

<%- include('./partials/footer')%>